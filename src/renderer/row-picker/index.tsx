import React from 'react';
import { createRoot } from 'react-dom/client';
import { RowPickerApp } from './RowPickerApp';

const root = createRoot(document.getElementById('root')!);
root.render(<RowPickerApp />);
